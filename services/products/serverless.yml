service: seed-test-products

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: sa-east-1

functions:
  hello:
    handler: handler.hello
    events:
     - http:
        path: products
        method: get